---
- name: Cấu hình trên router
  hosts: all
  gather_facts: no

  tasks:
    - name: Show IP
        commands:
          - "/interface print"
      register: output

    - debug: var=output.stdout_lines
